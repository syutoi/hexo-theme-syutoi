# F2：评论插槽与 Waline 验收

日期：2026-09-22。环境：Node 20.19.2、pnpm 9.0.4、Chrome 151.0.7922.108 / Linux。

## 完成内容

默认关闭的共享评论容器与独立 adapter 接口，本地锁定的 Waline 3.13.0，文章和普通独立页开关，稳定线程路径，延迟资源加载、重试和无 JS 回退。页面默认风格与核心资源保持不变；示例站没有配置真实评论服务。

## 自动化验证

- `pnpm lint`、`pnpm clean`、`pnpm typecheck`、`pnpm build`、`pnpm test` 全部通过。
- 56 项测试通过；示例站生成 112 个文件，19 个页面检查、20 个示例链接以及 Markdown 检查通过。
- 新增配置与插槽范围测试；真实 Hexo 检查文章/Page、全局关闭、Front Matter 关闭、无效服务、中文/英文、子目录资源和线程路径；许可证清单与锁定依赖一致。
- `toolbox/check-comments.mjs` 的 12 个场景通过。1440/390/320px × Light/Dark 无横向溢出，键盘加载后焦点进入容器，失败后返回重试按钮。
- 点击前无 Waline 或服务请求；点击后使用真实客户端和本地模拟 API。重复挂载只有一个编辑器，资源不会重复下载。CSS/JS 503 可重试；API 503 可重新加载；验证提交内容和 `/blog/article/` 线程标识。
- 入口脚本失败、无 JS、单页关闭、全局关闭、列表页回退正常。无外部网络请求，无未捕获脚本错误。
- 已查看桌面明色与 320px 暗色截图，确认评论表单布局。截图保留在本机 `/tmp/syutoi-f2/`，结构化报告见 [f2-browser.json](f2-browser.json)。

## 资源

以下为 gzip level 9 的字节数；默认关闭不下载评论资源。完整报告：[f2-budget.json](f2-budget.json)。

| 资源 | gzip 字节 |
| --- | ---: |
| `source/js/syutoi.min.js` | 2028 |
| `source/css/syutoi.min.css` | 5408 |
| `source/js/comments.min.js` | 817 |
| `source/css/comments.min.css` | 368 |
| `source/js/waline.min.js` | 73745 |
| `source/css/waline.min.css` | 5023 |

核心 JS/CSS 与 F2 开始前完全一致。Waline 的框架依赖只进入独立 bundle；当前预算分别为入口 JS 4 KB、插槽 CSS 2 KB、Waline JS 100 KB、Waline CSS 10 KB。

## 验证边界

没有部署、操作或向真实 Waline 服务提交评论。本地模拟服务不能代替真实环境的 CORS、登录、数据库、审核、通知和反垃圾验证。第三方客户端自身的全部交互无障碍未做完整审计；本阶段覆盖主题插槽的键盘操作、状态反馈和窄屏布局。真实评论正文及头像可能引用外部资源。
