# F3：SEO 与结构化社交链接验收

日期：2026-09-22。Node 20.19.2 / pnpm 9.0.4。基线为 0.3.0 标签 `c902412`。

## 本次变化

构建时的 SEO 配置、单页覆盖、Open Graph/Twitter Card 标签控制、分享图片回退和替代文字、页面 locale、文章分类标签、noindex 控制，以及 PRD 的结构化社交链接写法。所有变化在 Hexo 渲染阶段完成，无新增客户端脚本或 CSS。

## 验证

- `pnpm lint`、`pnpm clean`、`pnpm typecheck`、`pnpm build`、`pnpm test` 通过。
- 61 项测试通过；112 个示例站文件生成；19 个页面检查、20 个示例链接及 Markdown/预算检查通过。
- 新增 5 项测试：SEO 配置/账号/locale 校验、搜索和 404 的固定 noindex、真实 Hexo 分享覆盖与文章分类标签、开关和无效 URL/图片处理、结构化社交链接兼容。
- 验证根目录与 `/blog/` 部署、繁体中文与 en-GB、图片 alt 与选中图片对应、页面正文标题不受 SEO 标题覆盖影响、HTML 转义、外部 canonical 去除片段且与 og:url 一致、默认图和显式禁用图片。
- 既有分页 canonical、文章时间、摘要转义、主题品牌与站点身份分离等测试继续通过。
- `git diff --exit-code -- source/js source/css` 通过；所有客户端产物与基线完全相同。核心 JS 2028 字节 gzip、CSS 5408 字节 gzip。

本阶段直接验证生成 HTML，不重复运行无变化的浏览器布局矩阵。未向第三方分享平台提交页面；平台抓取、图片可达性与缓存由部署环境决定。没有生成猜测的作者主页、翻译页面关系或 JSON-LD。RSS/Sitemap 的插件验收由 F4 完成。

规范依据：[Open Graph protocol](https://ogp.me/)。新增字段与优先级详见 [配置说明](../configuration.md#seo-配置与单页覆盖f3)。
