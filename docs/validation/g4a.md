# G4a：移动首页 LCP 定位与复测

日期：2026-09-22。基线提交 9ae4a0b；最终测量包含本批未提交的封面副本与维护脚本改动。沿用 [G4 的方法与限制](g4.md)，不覆盖原始报告。

## 定位过程

G4 优化后移动首页 LCP 中位数为 2.557s（2.556–2.563s），hero 图片仍是 LCP。HTML 图片发现和 fetchpriority 已通过检查；Lighthouse 的“渲染等待”归因并不足以判断具体原因。

先复制生成站点到 `/tmp/syutoi-g4a-probe-site`，用同一 4173 地址、Lighthouse 12.8.2 mobile 默认模拟节流分别试验。每个候选只做一次探索测量，不把这些单次值当作最终验收：

| 隔离候选 | 变更 | LCP | 决定 |
| --- | --- | ---: | --- |
| 同步解码 | 仅把首页 hero decoding=async 改为 sync | 2.558s | 与 G4 基线接近，不采用 |
| 封面质量 70 | 还原 HTML，仅重编码置顶文章封面 | 2.408s | 视觉检查后采用，另做完整重复测量 |

探索数据及原始报告哈希见 [候选记录](g4a-probes.json)。这说明减少同时加载的封面传输量是当前样本站点的有效优化方向，不等于证明图片解码或网络竞争在所有环境下的具体占比。

## 最终改动

- `wallpaper-2311325.webp` 仍为 800×1075，从原始 JPG 重新编码，cwebp 1.3.2、method 6、quality 78→70；没有对旧 WebP 二次转码。
- 文件由 128642 降至 108796 bytes，减少 19846 bytes（约 15.43%）。原图、图片内容、路径、尺寸与裁剪规则保持；q70 是有损编码，不声称逐像素相同。
- 维护工具为每张图片显式记录质量参数，另外两张图片仍使用 q78，重新生成内容未变。
- 主题模板、同步/异步解码规则、核心 JS/CSS、依赖、用户图片接口均未改。只是示例站图片维护，不会自动优化使用者的图片。

## 测量方法

构建后重新用 Python 静态服务提供真实 example/public（不再使用探索副本），沿用 G4 的 Node 20.19.2、Lighthouse 12.8.2、Chrome 151.0.7922.108、根地址 127.0.0.1:4173。首页/长文 × mobile/desktop × 每组 3 次新 Chrome profile，串行执行，不与构建或浏览器验收并行。模拟网络/CPU、视口、DPR 保持 G4 设置，无生产压缩/缓存，不屏蔽请求、不替换空页面。

## 工程与视觉回归

- `pnpm build`、`pnpm lint`、`pnpm test` 通过：67 项测试、示例/Markdown/订阅检查和全部资源预算通过。
- 核心 JS/CSS 仍为 2028/5419 bytes gzip，文件哈希与 G4 相同；无 TypeScript 或运行时代码变更，不重复 typecheck 或 INP 测量。交互测量仍参考 G4，不能称为本批新测结果。
- 原有浏览器脚本 176 组响应式、66 组无 JS 回归通过，页面脚本错误为 0。历史友链的外部头像仍被拦截；不是本次新增资源。
- 对照 q78/q70 的完整封面，并查看 390px 浅色首页、1440px 深色首页截图，颜色、主体和卡片裁剪未见明显问题；截图保存在 `/tmp/syutoi-g4a-browser/`。这是人工视觉抽查，不是图像无损证明，也不覆盖所有真机/DPR。
- 本地 4000 预览中的封面内容已核对为新副本；源 JPG 保留。没有发布站点、推送分支或移动版本标签。

浏览器明细见 [回归结果](g4a-browser.json)。

## 完整复测结果

| 页面 / 模式 | Performance 中位数 | G4 → G4a LCP 中位数（秒） | G4a LCP 范围（秒） | CLS 中位数 | TBT 中位数（毫秒） |
| --- | ---: | ---: | ---: | ---: | ---: |
| home / mobile | 97 | 2.557 → 2.335 | 1.964–2.406 | 0.04850 | 7 |
| home / desktop | 100 | 0.611 → 0.557 | 0.510–0.574 | 0.00041 | 64.5 |
| article / mobile | 99 | 1.730 → 1.730 | 1.655–1.733 | 0.04850 | 0 |
| article / desktop | 100 | 0.519 → 0.508 | 0.467–0.516 | 0.00039 | 0 |

12 次 LCP 均 <2.5s、CLS 均 <0.1，采集到的请求均为本地资源且 HTTP 200。**移动首页第三次出现 CPU 性能低于 Lighthouse 预期的警告**；该次 LCP 为 1.964s，CLS 为 0，不能将其较好结果当作确定的优化收益。表中保留全部三次，不移除异常或替换为更好样本。

完整环境、原始请求、指标、警告、诊断及逐次 SHA-256 见 [性能记录](g4a-performance.json)。原始 Lighthouse JSON 保存在 `/tmp/syutoi-g4a/`。由于 CPU 警告，另外对移动首页完整追加三次同条件确认，单独记录，不与上表混算中位数。

复现：构建并按 G4 启动 4173 静态服务，运行下面命令；每次指定新目录保存历史结果。

```bash
SYUTOI_LIGHTHOUSE_CLI=/tmp/syutoi-lighthouse-d8/node_modules/lighthouse/cli/index.js \
SYUTOI_PERFORMANCE_OUTPUT=/tmp/syutoi-g4a \
node toolbox/check-performance.mjs
```

## 独立确认与结论

追加三次移动首页测量均无运行警告：LCP 为 2.134 / 2.408 / 2.405s，中位数 **2.405s**，均低于 2.5s；CLS 均低于 0.1。逐次设置、指标和哈希见 [独立确认记录](g4a-confirmation.json)，原始报告在 `/tmp/syutoi-g4a-confirm/`。

追加组使用原 CLI、相同 URL 与 mobile 参数，只重新启动三次独立导航；没有降低节流强度或替换页面。它支持当前条件下改善可复现，不能据此保证所有设备/网络下达标，也不能把两组波动全部归因于这张封面。G4 的实验室 INP 会话结果未在本批重新测量，现场 p75 仍未知。

G4a 完成：核心预算不变，当前两个代表页面的 LCP/CLS 在完整矩阵中均低于目标，移动首页追加确认同样达标。下一项 G5：Demo/文档站与安装方式验证。
