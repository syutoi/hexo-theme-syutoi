# G2：迁移兼容清单验证

日期：2026-09-22。Node 20.19.2 / Hexo 8.1.2。代码基线：f00d80a。

## 核对范围

- 对照 `git show a2a8440:_config.yml` 的全部顶层配置组，以及当前 `lib/config.cjs`、主题默认配置、标签注册器和既有配置文档，发布 [迁移清单](../../example/source/_posts/theme-docs/migration-from-shoka.md)。
- 区分兼容别名、手动迁移、移除功能；列出主题三个标签注册入口 links、linksfile、media，区分 Hexo 内置标签与其他插件标签。
- 给出私有 Markdown 替代写法、只读扫描与迁移顺序。不会自动改写历史文章、第三方插件或旧服务端数据。

## 验证结果

- `node --test test/config.test.mjs test/view.test.mjs test/config-render.test.mjs`：32 项通过，包括旧配置优先级、真实 Hexo 渲染、子目录资源和标签安全输出。
- `node toolbox/check-markdown.mjs`：现有示例产物中的标题、脚注、任务列表、表格、折叠、图片、代码与历史内容检查通过；此次未改源码，不重新生成产物。
- 临时 Node 校验读取文档的 3 个 YAML 片段，解析成功；通过实际 normalizeConfig 对照迁移前后的 branding、appearance、navigation 及社交 URL。
- 从文档提取 2 个标签示例，调用实际 links/media 注册函数，确认生成链接、媒体路径带 /blog/ 前缀。首次临时断言遗漏 HTML 实体解码，修正校验后通过；主题输出本身正确。
- 文档中 11 个本地文件链接目标存在，`git diff --check` 通过。

本次仅修改文档，没有增加兼容代码、依赖或客户端资源；未重跑无变化的全量浏览器矩阵。扫描命令用于人工盘点，不是完整语法解析器；其他 Shoka 分支和第三方插件需另外核对。下一项 G3 执行核心页面响应式与无障碍验收。
