# G1：文档与安装路径验证

日期：2026-09-22。Node 20.19.2 / pnpm 9.0.4 / Hexo 8.1.2。代码基线：53703fe。

## 文档范围

六份指南齐备：getting-started、configuration、writing、customization、deployment、migration-from-shoka。新增源码定制和两种项目布局的部署说明，更新搜索/评论/SEO/Sitemap 的开关与边界，README 提供入口。迁移表的完整盘点仍作为 G2 单独推进。

## 实际执行

- 在 `/tmp/` 下新建独立博客，使用 `git clone --no-hardlinks` 从当前本地仓库复制主题，避免依赖尚未推送的远端更新。
- 按文档安装主题生产依赖和博客 Hexo/renderer/category/tag/server 依赖，没有链接仓库 node_modules。主题使用冻结锁文件，独立博客生成锁文件后再次 frozen 安装成功。
- 直接读取快速开始的 YAML 片段作为博客配置；运行 `hexo new page about`、`hexo new`、`hexo clean` 与 `hexo generate`。
- 根目录版本生成 37 个文件，首页、文章、Page、归档、分类、标签、404 和版本资源可用；正文粗体、静态任务列表、脚注输出正确，默认不引用搜索/评论入口。
- 修改为 url=https://example.com/blog、root=/blog/，开启 Pagefind，再次 clean/generate；资源路径与 canonical 正确，搜索页和引擎存在，索引含 3 条记录。source/CNAME 正确复制到 public/CNAME。
- 六份文档及 README 的 25 个 YAML 示例解析通过；50 个本地文件链接存在。部署 workflow 示例作为 YAML 解析，未在 GitHub 上运行。`git diff --check` 通过。

生产安装过程中存在当前锁定依赖的 peer 范围提示，安装和实际生成通过；不把此结果当作所有平台兼容保证。临时站点的 example.com 仅作为生成地址，没有向该域名发送请求或发布。

## 未覆盖

没有执行远端 clone/标签发布、GitHub Actions 部署、DNS、证书或平台权限操作；从空目录开始的 Hexo CLI init 引导依据官方流程，未在本次夹具中运行。CLI 的文章/Page 创建、依赖安装与生成已实际执行。

本次仅修改 Markdown，没有新增配置接口、源码或构建产物，不重复运行全量代码/浏览器测试。主仓库现有预览保持运行。

部署说明依据仓库现有 workflows、[GitHub Pages 官方说明](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages) 和 [Hexo Pages 指南](https://hexo.io/docs/github-pages)。
