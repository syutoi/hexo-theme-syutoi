# Syutoi 开发任务

依据：[产品需求文档 v0.1](<hexo-theme-syutoi 产品需求文档（PRD）v0.1.md>)。

执行顺序：先建立可以运行、可以验证的旧主题基线，再按 PRD 重写。
每完成一个批次更新此文件；只有实际实现并通过验收的任务才勾选。
PRD 第 52 节的 v0.1 MVP 验收标准优先于后续版本功能清单。
当前包版本为 `0.5.0`：`v0.1.0` 标记 MVP，`v0.2.0` 标记 E 阶段阅读体验，`v0.3.0` 标记 F1 搜索与 F2 评论完成后的版本整理提交，`v0.4.0` 收录 F3/F4 与 G1–G4a 的成果；`v0.5.0` 收录木色视觉、田园配图、文档中心、Markdown 扩展及 G5a–G5d 已完成的验收。实际版本号按开发里程碑确定，不再逐一对应原 PRD 的阶段版本；此前 `0.2.5` 是上游遗留版本。

## A. 可运行基线（已完成）

- [x] A1：将当前主题名称、配置、路径和文档目录改为 Syutoi；保留上游来源和原站外链。
- [x] A2：保留原 MIT License，并添加 NOTICE 说明代码来源（PRD §31）。
- [x] A3：升级示例站到 Hexo 8，声明 Node >=20.19，建立 pnpm workspace 与锁文件；明确主题直接依赖。
- [x] A4：提供根目录 `pnpm dev/build/clean/test`；自动连接示例站到当前主题，避免手工复制。
- [x] A5：修复在真实构建中发现的配置、路径或依赖兼容问题。
- [x] A6：完成 clean → generate → 产物检查，启动本地服务并验证主要页面与 CSS/JS HTTP 响应。
- [x] A7：加入 CI，在 Node 最低支持版本与 LTS 版本执行锁定安装、构建和产物检查。
- [x] A8：更新 README，记录开发命令、运行要求及当前旧主题基线的局限。

验收：新 checkout 按 README 安装后可运行；首页、文章、独立页、归档、分类和标签页及静态资源可访问；构建日志没有错误。此阶段不代表现代化 MVP 已完成。

## B. v0.1 工程重建（已完成，PRD §8–12、37–41、52）

- [x] B1：建立 `src/client`、`src/styles`、`src/shared`，以 TypeScript + esbuild 构建客户端，添加有实际输入的 `typecheck`。
- [x] B2：建立 CSS + Custom Properties + PostCSS 流程；定义颜色、宽度、间距、字号和圆角 tokens。
- [x] B3：将开发监听与 Hexo 预览串联；生产输出进入 `source/`，明确生成文件的版本控制策略。
- [x] B4：逐步替换 Stylus 和旧脚本；切换前后都验证示例站，切换完成后移除旧构建依赖。已按反馈恢复接近原版的视觉结构，并重新完成本地验证。
- [x] B5：替换 multi-markdown-it 定制渲染器为通用 Markdown renderer；盘点旧示例语法并记录迁移差异。
- [x] B6：简化配置至约 100–150 行、最多三层；导航与社交链接采用结构化 YAML。
- [x] B7：引入适量 lint/typecheck；只为配置、URL 等独立逻辑添加必要单元测试，并纳入 CI。

验收：主题构建、类型检查、示例站构建均通过；核心不依赖 Stylus、自建 Markdown renderer 或第三方运行时框架。

## C. v0.1 核心页面（已完成，PRD §13–16、18、24、26、34–36、52）

- [x] C1：重建语义化 Nunjucks 基础模板、Header/Footer 和内容容器。
- [x] C2：实现轻量首页与文章列表，支持分页、可选摘要和可选封面。
- [x] C3：实现文章页、独立 Page、归档页，正确显示标题、日期和主要内容。
- [x] C4：实现中文、英文和混排排版；默认系统字体，正文宽度约 720px。
- [x] C5：提供本地 SVG 图标，移除远程 Iconfont 和默认 Google Fonts 请求。
- [x] C6：实现移动导航、响应式布局、键盘焦点和主要页面的无 JS 可用性。
- [x] C7：保留并整理 zh-CN、zh-TW、en 文案，去除核心界面的硬编码文本。
- [x] C8：整理页面 title、description、canonical 与基本 Open Graph；避免继承示例站作者身份。

验收：桌面与移动端可阅读和导航；无封面、长标题、中英文文章正常；核心页面在禁用 JS 时仍可访问。

## D. v0.1 阅读、主题与收尾（PRD §17、20–21、32–34、42、52–53）

- [x] D1：实现 light/dark/auto，读取系统偏好、保存用户选择，并在首次绘制前应用主题。
- [x] D2：覆盖 H1–H6、段落、列表、引用、链接、表格、图片、代码、脚注和 details 样式。
- [x] D3：以 renderer 输出为基础提供代码语言标签、复制按钮、横向滚动及深浅配色。
- [x] D4：实现图片响应式、原生懒加载、标题说明与合理尺寸约束。
- [x] D5：完善 MVP 示例文章、图片和表格，增加无封面、长文和边界情况。
- [x] D6：移除默认 PJAX、音乐、烟花、评论、搜索、打赏、动画和统计脚本；默认无第三方 JS 请求。
- [x] D7：验证 Light/Dark、Mobile/Desktop、无 JS、键盘操作和 reduced-motion；记录实际结果。
- [x] D8：统计核心 JS/CSS gzip 大小；使用 Lighthouse 测量 LCP/CLS/INP 相关指标并记录测试环境，避免把目标写成实测结果。
- [x] D8a：根据 D8 实测优化示例图片传输与响应式资源，保留现有视觉效果；同条件复测移动 LCP，并记录仍未验证的真实 INP。
- [x] D9：补齐配置、开发和基础写作说明，更新版本与 CHANGELOG，逐项完成 PRD §52 验收。

验收：`pnpm build`、类型检查、必要测试和 `hexo generate` 通过；核心页面完整、Light/Dark 可用、无默认第三方 JS。v0.1 暂不集成 PRD §53 列出的功能。

## E. MVP 后：完整阅读体验（已完成，实际版本 0.2.0）

- [x] E1：补齐分类页、标签页、404 和文章前后篇导航（PRD §13）。
- [x] E2：实现桌面 sticky TOC、移动端折叠目录和可渐进增强的 scroll spy（§19）。
- [x] E3：完善文章元信息、阅读时间及封面可选体验。
- [x] E4：评估可选图片灯箱；用隔离原型验证关闭时不加载依赖，记录实际接入边界。
- [x] E4a：按 E4 决策接入默认关闭的可选灯箱，补齐下载失败回退与严格焦点约束，再验收资源隔离及无 JS 回退。

## F. 博客能力（0.5，PRD §22–25）

- [x] F1：定义搜索 provider 接口，验证 Pagefind 与 Hexo 生命周期的集成，再实现可选本地搜索。
- [x] F2：定义 Comments Slot，以 Waline 为首个可选 adapter；不启用时无请求。
- [x] F3：完善 Open Graph/Twitter Card、结构化社交链接和 SEO 配置。
- [x] F4：通过 Hexo 插件验证 RSS、Sitemap，避免在主题重复实现。

## G. Beta 与稳定版（0.9–1.0，PRD §43–51）

- [x] G1：编写 getting-started/configuration/writing/customization/deployment/migration-from-shoka 文档。
- [x] G2：发布配置与内容迁移兼容清单，明确不支持的旧标签及替代写法。
- [x] G3：完整验证首页、文章、Page、归档、分类、标签、404 的响应式与无障碍。
- [x] G4：验证性能预算：核心 JS gzip <50KB（争取 <30KB）、CSS gzip <40KB；按明确环境记录性能数据。
- [x] G4a：定位移动端首页 LCP 的剩余瓶颈，在保留现有视觉布局的前提下优化，并按 G4 同条件复测（G4 基线 2.557s，G4a 独立确认中位数 2.405s）。
- [ ] G5：完成 Demo／文档站与分发验收（进行中）。
  - [x] G5a：验证当前开发提交的独立 Git 安装、生产依赖及根目录/子目录；见 [安装验收](validation/g5a.md)。
  - [x] G5b：完成 Demo 文档中心、固定链接与仓库文档同步（见下方已完成记录）。
  - [x] G5c：验收线上 Demo／文档、静态资源、404 HTTP 状态、订阅和 canonical；见 [线上验收](validation/g5c.md)。
  - [x] G5d：基于新图片、配色与文档数据集重新测量性能，记录新基线；见 [性能报告](validation/g5d.md)。
  - [ ] G5d1：隔离验证图片传输成本并优化移动首页 LCP；G5d 中位数 3.013s，尚未达到 <2.5s。
  - [ ] G5e：明确 npm 分发取舍；当前只支持 Git 安装，npm 仍属可选，不视为已实现。
- [ ] G6：整理版本策略、发布流程、许可证和依赖，准备正式 1.0 发布。

## 验证记录

### 2026-09-17：运行基线

- 环境：Node 20.19.2、pnpm 9.0.4、Hexo 8.1.2。
- `PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true pnpm install`：已完成；锁文件已生成。
- 再次执行 `pnpm install --frozen-lockfile`（同样跳过 Chromium）：通过。
- `pnpm clean && pnpm build && pnpm test`：通过；生成 79 个文件，检查 8 类页面、正文、JS 语法、静态资源和 RSS/Atom/JSON feed。
- `pnpm dev`：监听 `127.0.0.1:4000`；首页、文章、独立页、归档、分类/标签列表及详情、404、CSS、JS、JSON feed 共 12 个地址返回 HTTP 200。
- 已修复：示例 audio 的 YAML 缩进、链接主题时递归扫描、Hexo 8 移除的 gist/jsfiddle/youtube/vimeo 示例标签、脚本资源硬编码路径，以及顶层变量压缩造成的浏览器名称冲突；移除失效的 polyfill 请求。
- 内置 CDN、字体及图标 URL 改为 HTTPS，避免本地 HTTP 预览沿用 HTTP 外链。
- Chrome 实测：1440px 桌面首页与 390px 移动端文章均完成加载，未报告未捕获的页面脚本错误；外部图片、字体加载仍受网络影响，完整视觉验收留待重写阶段。
- 示例 Mermaid/Graphviz 暂按代码展示，避免旧渲染器在构建中启动 Chromium。
- CI 工作流已添加，尚未在 GitHub 上执行；Node 24 的结果以后以 CI 实测为准。

遗留问题（后续工程重建中处理）：

- 上游 renderer 仍包含弃用依赖，Prism diff 插件存在非致命提示。
- `hexo-feed` 的 peer 范围尚未声明 Hexo 8；当前三类 feed 已通过实测。Nunjucks/Chokidar 仍有 peer 警告，后续升级或替换时需要消除。
- 旧界面仍依赖 CDN、外部图片和旧功能；无 JS 可用性、零默认第三方请求和性能指标尚未验收。
- 当时的下一批计划为 B1–B3；完成记录见下。


### 2026-09-17：B1–B3 构建链与主题偏好

- 完成 TypeScript 严格检查、esbuild IIFE 打包、PostCSS imports/Autoprefixer 与 design tokens；配置、源码和开发说明已加入仓库。
- 新产物为 `source/js/syutoi.min.js` 和 `source/css/syutoi.min.css`，随源码纳入版本控制；CI 增加类型检查、单元测试和产物一致性检查。旧布局仍走原来的构建链。
- `pnpm dev` 串联首次资源构建、资源监听和 Hexo 服务。实际验证了 CSS import 更新、TS 更新、语法错误保留旧产物、恢复后继续构建，以及 SIGTERM 后端口释放。
- 原深浅色控制器已迁入新客户端（提前完成 D1）：light/dark/auto、启动时应用偏好、键盘操作、本地 SVG、持久化、系统跟随、跨标签页同步、旧偏好兼容和受限存储回退。
- Chrome 对新客户端进行了隔离验证（阻断旧 app.js 与外部请求）：在 body 解析前设置模式；键盘切换、重新加载、显式偏好不被系统覆盖、auto 跟随系统、跨标签同步和禁止 localStorage 均通过。此检查不代表完整旧页面已消除外部依赖。
- `pnpm typecheck`、完整示例站构建（81 个文件）、7 项单元/构建测试及 8 类页面产物检查通过。构建失败测试会故意构造语法错误；其报错属于测试输入，测试本身应通过。
- 锁定安装通过；CI 配置已更新，Node 24 与远端 CI 的结果仍待实际运行。

下一批：继续 B4/B5，迁移旧脚本与 Stylus、替换定制 Markdown renderer；随后完成 B6 配置整理及 B7 剩余 lint。B4/B7 本批只有部分进展，尚未勾选。完整页面、排版和无 JS 验收仍按 C/D 阶段推进。


### 2026-09-18：B4 页面资源切换

- 已将所有核心模板切换为原生 CSS / TypeScript；移除 Stylus、旧 app.js 生成器及 PJAX、播放器、烟花等运行时，并从示例依赖及锁文件移除 Stylus renderer 和 Hexo Autoprefixer。主题构建仍使用 PostCSS Autoprefixer。
- 为解除旧运行时耦合，同步完成基础语义模板、Header/Footer、内容容器（C1）、本地 SVG 与系统字体（C5）、默认第三方功能移除（D6）。首页、文章、归档和移动导航已具备新版基础实现；其余 C/D 项继续按各自完整范围验收。
- 友情链接保留原生链接与图片；旧媒体列表转换为普通链接。标题和配置值默认转义；封面改为显式可选，取消随机图片。见 [迁移说明](migration-from-shoka.md)。
- 切换前基线构建及 7 项测试通过；切换后 `pnpm typecheck`、示例站构建（75 个文件）、11 项单元/构建恢复测试和 8 类页面产物检查通过。新增测试覆盖 URL、旧/新菜单、置顶分页、空首页及子目录资源/内容链接。
- 本地 Chrome 实测 10 类页面：桌面 1440px、移动 390px 无横向页面溢出；关闭 JS 后正文和移动导航可用；键盘菜单、Escape 焦点返回、复制正文代码（不带行号）、深浅色切换通过。阻断外部资源时无页面脚本错误，未发出第三方脚本、字体、样式请求。作者正文的外部图片仍可能请求网络。
- 已检查桌面与移动端截图。完整 Lighthouse、更多排版边界及 Node 24 / 远端 CI 尚未运行，留在原任务中。
- 定制 renderer 与 Prism diff 的非致命提示仍保留；旧示例说明不代表全部旧功能仍被支持。

下一批：B5，替换 `hexo-renderer-multi-markdown-it`，盘点示例语法并记录迁移差异；随后 B6 精简配置。

### 2026-09-18：B4 视觉验收反馈

- 用户反馈新版与原版差异过大、界面简陋。上一批将工程迁移扩大为整体视觉重设计，技术检查通过不能代替视觉验收。B4 恢复为未完成。
- 暂停 B5；先确认视觉保留范围，修正首页构图、文章卡片、封面与信息层次，再检查桌面/移动端效果。
- PRD §14 的简约方向与“无封面仍应很好看”需要同时满足，不能仅以删除视觉元素作为实现。


### 2026-09-18：B4 视觉修正与提交策略

- 按用户确认的方向恢复接近原版的图片页头、静态波浪、交错图文卡片、分类入口、作者侧栏，以及文章内容面板和普通目录链接。保留 TypeScript / PostCSS 构建及无 JS 可读性。
- 页头通过 `appearance.cover` 显式配置；示例站复用本地图片，三篇文档文章设置明确封面，其他文章保留无封面卡片。未恢复随机图片、CDN 脚本、PJAX 和播放器。
- 本地检查 Light/Dark 桌面首页与移动页面截图，并复测主要页面、键盘菜单、代码复制和禁用 JS 的导航。技术与截图检查记录不代表用户已最终认可所有视觉细节，C/D 阶段继续完善。
- 后续每完成一个大任务，在检查通过后创建一次 Git 提交，提交信息使用英文。当前提交以 B4 为边界，B5 单独执行和提交。


### 2026-09-18：B5 通用 Markdown renderer

- 示例站改用 `hexo-renderer-markdown-it@7.1.1`，显式启用脚注与 `markdown-it-task-lists@2.1.1`；代码使用 Hexo 内置 Highlight.js，未知语言回退纯文本，保留复制按钮。
- 移除 `hexo-renderer-multi-markdown-it` 及其 Puppeteer / deasync 依赖链、旧 minify 配置和不用的 Pangu 模板过滤器；CI / 安装说明不再设置跳过 Chromium 下载的环境变量。HTML 不再额外压缩，JS/CSS 仍由主题构建压缩。
- 盘点原示例私有语法；Java 笔记和普通历史说明中的提示块改为引用，题目改为普通文本。复杂特殊功能历史页保留原文并明确标注支持范围，保留历史来源链接。具体差异见 [迁移说明](migration-from-shoka.md)。
- 新增 `/reading/` 写作示例，覆盖脚注、任务列表、表格、代码、未知语言转义、原生图片懒加载、details、ruby 与重复标题；新增实际 HTML 产物检查并纳入 `pnpm test`。
- 类型检查、76 个文件的示例站构建、11 项单元/构建恢复测试、9 类页面及 Markdown 产物检查通过。新 renderer 构建日志无旧 Prism diff 提示。
- Chrome：10 类页面在 1440px / 390px 与无 JS 下复测通过；写作示例另测 Light/Dark、精确代码复制、脚注、图片与无 JS 原生折叠。已查看移动端正文截图；未改变 B4 修正后的页面外壳。
- 仍存在 Nunjucks/Chokidar 与 hexo-feed/Hexo 的 peer 范围提示；本地构建通过，Node 24 / 远端 CI 尚未运行。

下一批：B6，简化主题配置并统一导航与社交链接的结构化 YAML；作为独立任务验证与提交。


### 2026-09-18：B6 配置整理

- 默认主题配置从 309 行精简为 111 行（含说明），示例覆盖为 24 行；只列出实际接入模板的字段，最多三层。新增 [配置文档](configuration.md)，说明 Hexo 覆盖顺序、完整列表替换、空值、有效 URL 及旧字段映射。
- 导航统一为 `navigation.menu` 的 `{ name, url }` 列表；`menu.*` 标签按语言翻译，普通标签保留原文。社交链接也使用结构化列表，显示为作者侧栏中的普通链接；默认不含上游作者账号。
- 展示名、头像、网站图标统一放入 branding，接入侧栏和页脚开关。空菜单不再输出空导航或菜单按钮；关闭侧栏时使用居中单列布局。
- 兼容旧 alternate、darkmode、menu、sidebar.avatar/images 和 social 映射；站点显式旧字段不会被新默认值遮盖，同一覆盖配置中显式新字段优先。非法 URL 和无效链接条目被忽略，配置文字默认转义。
- 类型检查、干净示例站构建、19 项测试、9 类页面和 Markdown 产物检查通过。其中 3 项测试创建隔离 Hexo 站点，实际验证新/旧配置、子目录 URL、空菜单、禁用侧栏和页脚、社交链接转义及年份区间。
- Chrome 回归：10 类页面桌面/移动端、无 JS 导航，以及写作页 Light/Dark、复制、脚注、图片、原生 details 均通过。

下一批：B7，补齐适量 lint / 工程检查并纳入 CI；独立验证与提交。


### 2026-09-18：B7 工程检查

- 增加 `pnpm lint` 和 ESLint flat config：ESLint recommended 覆盖 JS，typescript-eslint recommended 覆盖 TS；沿用严格 TypeScript 检查。Node / 浏览器 / Hexo 脚本分别声明运行环境，Hexo 全局仅限主题脚本。
- lint 检查 29 个手写源码文件，排除 example、生成资源、依赖与 coverage；不强制整库格式化。未使用的规则禁用注释报错，警告也导致命令失败。
- 清理未使用的变量和回调参数，明确构建测试中的资源初始状态；URL 控制字符拒绝规则保留，并添加局部、带原因的 lint 例外。
- 已使用临时输入验证 JS 未定义名称、TS 显式 any 能被拦截，确认主题链接和生成产物被排除；没有增加重复实现的测试。
- CI 增加 lint，开发文档和 README 更新检查顺序。锁定安装、lint、clean、typecheck、76 文件构建、19 项测试、9 类页面与 Markdown 产物检查全部通过。
- 重建后的已提交 JS/CSS 与本批开始前完全一致，本批不改变界面。远端 CI 与 Node 24 仍待实际执行。

B 阶段完成。下一批：C2，完善首页与文章列表的分页、可选摘要和封面，保持已确认的视觉方向；独立验证与提交。


### 2026-09-18：C2 首页与文章列表

- 保留当前页头、波浪、分类卡片、交错图文列表与作者侧栏。新增 `post_list.summary`、`summary_length` 和 `cover`，统一控制首页、分类和标签列表，默认保持开启。
- 支持文章 `summary` 自定义文字或 `false` / 空字符串隐藏摘要；未指定时依次使用 description、more 标记前内容和正文。摘要移除 HTML 标签、还原常用实体并按 Unicode 码点截断，模板继续转义文字。列表设置不影响文章页封面、正文和 SEO 描述。
- 空摘要不再输出空段落；同时没有摘要与封面的卡片取消最小高度。默认图文布局保持不变，长标题自然换行。
- 补充真实 Hexo 生成测试：三页文章的日期/置顶顺序、无重复或遗漏、子目录分页、空站点、不分页、摘要优先级/关闭/转义与封面开关；增加配置边界检查。
- lint、typecheck、干净构建（76 文件）、24 项测试、9 类页面及 Markdown 产物检查通过。Chrome 验证首页三页在 1440/390 宽度与 Light/Dark 下无横向溢出，无 JS 可点击翻页；临时 DOM 场景验证无图无摘要卡片及超长标题。原有 10 类页面、导航键盘、复制、主题切换及无 JS 回归通过。
- 更新 [配置文档](configuration.md)，说明列表选项、单篇覆盖和 Hexo 分页规则。默认配置仍控制在 150 行以内（现为 120 行）。

下一批：C3，完善文章阅读页；保持当前视觉方向，独立验证与提交。


### 2026-09-18：C3 文章、独立 Page 与归档

- 文章页补齐分类链接，作者优先使用单篇 author；更新时间与发布日期不在同一天时才显示。末尾显示作者年份和永久链接，不预设转载许可；标签与相邻文章导航在有内容时输出，预留默认空的 comments 模板 block。
- 独立 Page 支持可选封面和原生目录链接，保持标题/正文布局，不混入文章元信息。文章和 Page 均可通过 Front Matter `toc: false` 关闭目录；滚动高亮与折叠仍留待 E2。
- 修复归档首页忽略 per_page 的问题；归档首页、年份和月份页统一分页。空站点生成可访问的归档空状态，yearly / monthly / daily 可独立开关，每日归档标题包含日。
- 新增 4 项真实 Hexo 生成测试，覆盖作者/分类/标签/日期/来源/邻接文章、独立 Page 及目录、跨年份归档分页与空状态、关闭年份归档后的月/日归档；子目录链接与无重复文章同时验证。
- lint、typecheck、干净构建（78 文件）、28 项测试、9 类页面及 Markdown 产物检查通过。Chrome 验证 8 类相关页面在桌面/手机、Light/Dark 下无横向溢出，禁用 JS 后可翻归档页、点击独立 Page 目录和相邻文章。原有 10 类页面与导航、复制、主题回归通过，无浏览器脚本错误。
- 更新配置文档，说明文章字段、Page 创建方式、目录和归档规则；保留现有视觉风格。

下一批：C4，完善中文、英文与混排排版，检查系统字体及正文宽度；独立验证与提交。


### 2026-09-19：C4 中文、英文与混排排版

- 将既有 `--syutoi-content-width: 720px` 实际接入文章/独立 Page、正文和相邻文章导航；标题、封面与正文对齐，首页卡片及侧栏风格保持不变。
- 继续使用系统字体，增加本地简繁中文字体回退，不请求 Web Font。正文保留 1rem 字号、1.85 行高和 1.5rem 段落间距；补齐 H1–H6 的统一行高、上下标和注音尺寸。
- 使用自然单词边界、中文严格标点断行与必要时的长串折行，不插入空格或强制两端对齐；代码块单独保持原始行并在自身容器内滚动。
- 新增 `/typography/` 排版示例，包含简繁中文、英文长段落、混排、长标题、引用/嵌套列表、上下标/注音、长链接、行内标识符及长代码行，并纳入示例站产物检查。配置文档补充正文排版和 lang 标注说明。
- lint、typecheck、干净构建（79 文件）、28 项既有测试、10 类页面及 Markdown 产物检查通过；本批以真实页面检查验证 CSS，未增加重复样式实现的单元测试。
- Linux Chrome 验证 Light/Dark 下 1440、1024、768、390、320px 屏宽，正文实测依次为 720、606、420、318、248px，无页面横向溢出；临时关闭侧栏仍为 720px，根字号放大至 200% 及禁用 JS 后可读，代码容器保持横向滚动。无远程字体请求。桌面/手机截图人工检查、既有 10 类页面与写作页复制/脚注/details 回归通过；未实测其他操作系统字形。

下一批：C6，完善移动导航、键盘焦点、响应式与主要页面的无 JS 可用性；C5 已完成。


### 2026-09-19：C6 移动导航、键盘与无 JS

- 移动菜单支持 Enter / Space、Escape、Tab 自然离开及点击外部收起；收起前将菜单内焦点移到按钮，同页锚点继续原生跳转。断点切换时在按钮/链接之间转移焦点，记录最近焦点以处理 CSS 隐藏早于媒体查询事件的浏览器时序。
- 移动页头改为正常布局，较多导航项推开横幅，保留原有图片、波浪和配色；无 JS 时显示全部链接并隐藏增强按钮。导航链接最小高度为 44px，正文跳转目标恢复可见焦点框。
- Linux Chrome 实测键盘跳转正文、Space/Enter/Tab/Escape、断点切换、外部点击及同页锚点；临时增加 15 项导航，在 320/390/760px 和启用/禁用 JS 下无覆盖或横向溢出。10 类页面无 JS 导航通过，浏览器无脚本错误。
- 既有 10 类页面、主题和复制回归通过；C4 的 5 档屏宽、Light/Dark、720px 正文、200% 文字及无 JS 排版验证通过。开发文档记录可重复的键盘检查步骤与未验证平台范围。
- lint、typecheck、干净构建（79 文件）、28 项测试、10 类页面和 Markdown 产物检查通过。

下一批：C7，整理 zh-CN、zh-TW、en 文案及核心界面硬编码文本；独立验证与提交。


### 2026-09-19：C7 核心界面语言

- 将 zh-CN、zh-TW、en 整理为各 60 行的同名核心键集合，移除播放器、搜索、打赏等已退役功能的文案。保留 ja、zh-HK 历史翻译，缺失新键按配置顺序回退，不宣称完整本地化。
- 补齐友链导航、RSS 订阅提示、分类/标签空状态和带本地标点的分类/标签标题及来源标签；产品名与协议名保留原文，作者内容和自定义菜单文字不自动翻译。
- 新增 locale 解析与 template_locals 过滤器，在 Hexo 原生语言路由识别之后规范化内容语言，支持语言列表、下划线别名、zh-Hans/zh-Hant 和 en-US。翻译按页面语言、站点列表、英文逐键回退，移除无效 default.yml 占位字符串，避免依赖其他语言文件的加载顺序。
- 保留 source/_data/languages.yml 覆盖，重写语言说明并补充配置文档。新增测试验证语言标记与回退、三套键一致及模板引用完整性、真实 Hexo 的单页覆盖/语言列表/自定义转义文案，以及未知语言和空分类标签提示。
- lint、typecheck、干净构建（79 文件）、32 项测试、10 类页面及 Markdown 产物检查通过。独立 Hexo 站点生成 en/zh-CN/zh-TW 页面，Chrome 验证 1440/390px 下无溢出、复制按钮及状态、菜单、主题标签和无 JS 导航；既有 10 类页面回归通过，无脚本错误。

下一批：C8，整理页面 title、description、canonical 与基本 Open Graph，检查示例身份与实际站点配置；独立验证与提交。


### 2026-09-19：C8 页面 SEO 元信息

- 统一生成页面 title、description、canonical 和 Open Graph，分页标题加入本地化页码，首页不重复站点名称。标题与作者按字面文字转义，保留尖括号等特殊字符。
- 描述依次使用单页 description、more 摘要、正文、站点描述；空正文仍可使用显式描述，无描述时省略标签。共享纯文本处理保留 Unicode 和常用实体、去掉标签及 script/style 内容；列表摘要开关不影响元信息。
- canonical/og:url 使用当前实际路径和 Hexo URL helper，遵循 url、子目录和 pretty_urls，不将分页合并到首页。文章/Page cover 和列表固定页头可提供绝对分享图片，非法协议被忽略。
- 增加基本 Twitter Card、文章作者及 UTC ISO 发布时间/更新时间；独立 Page 不输出文章元数据，404 输出 noindex。所有身份来自博客配置或单篇 Front Matter，不读取主题包作者/示例 branding，也不新增外部请求。
- 增加纯文本边界测试与两组真实 Hexo 元信息测试，覆盖转义、无正文描述、当前作者、子目录 canonical/图片、pretty_urls、分页标题、时区转换、非法封面和 404。更新配置文档，明确示例域名为占位、生产 url/root 需自行填写，feed/sitemap 仍交由插件。
- lint、typecheck、最终干净构建（79 文件）、35 项测试、10 类页面及 Markdown 产物检查通过；另检查示例站 9 类页面 title 唯一、canonical/og:url 一致及基本卡片标签。未测试外部搜索引擎收录或社交平台抓取。

C 阶段完成。下一批：D2，完善 Markdown 阅读元素的样式与边界情况；D1 已完成，按大任务独立验证与提交。


### 2026-09-19：D2 Markdown 阅读元素

- 完善 H4–H6 字号层级、嵌套引用的响应式留白、定义列表间距、图片说明间距、details 展开/末尾留白与脚注目标高亮、返回链接点击空间。
- 普通表格保留作者指定的对齐方式，单元格顶部对齐且最小宽度 10rem，在窄屏自身滚动，避免中文长说明被压成逐字换行；代码高亮布局表格明确豁免最小宽度。
- 新增 `/reading-elements/` 完整样例，覆盖标题、段落、嵌套/任务/定义列表、多层引用、对齐/宽表格、图片说明、代码、重复脚注与嵌套 details。宽表格使用原生 tabindex、caption 和 scope 提供显式键盘焦点及语义，未引入客户端依赖。
- 样例纳入页面与 Markdown 产物检查，验证标题、表格语义、说明、重复脚注返回和嵌套折叠结构；配置文档补充写法与后续 D3/D4 边界。
- Linux Chrome 验证 1440/768/390/320px、Light/Dark 无横向页面溢出，H1–H6 层级、表格三种对齐及键盘滚动正常；禁用 JS 后 details 的 Enter/Space、表格方向键和重复脚注往返均通过。手机截图检查后增大表格列宽，再次验证通过；既有代码复制、脚注、图片、details 和 10 类页面回归通过。
- lint、typecheck、最终干净构建（80 文件）、35 项测试、11 类页面及 Markdown 产物检查通过。D3 代码交互与 D4 图片专项仍未标记完成。

下一批：D3，完善代码语言标签、复制、横向滚动及深浅配色，独立验证与提交。


### 2026-09-19：D3 代码展示与复制

- 将语言标签与具名、可聚焦的滚动容器移到页面生成阶段，无 JS 时仍可通过 Tab/方向键阅读长代码。使用已在依赖树中的 htmlparser2 10.1.0 作为直接构建依赖定位原始片段，只在片段外插入容器，保持 renderer 的实体、缩进、换行和高亮标记，不加载客户端高亮器。
- 复制从代码节点读取内容，将 br 还原换行，排除行号；支持空代码、行末空格和折叠内容。每个代码块提供可见且可播报的成功/失败状态，失败可重试；处理中阻止重复操作但保持按钮焦点，无 Clipboard API 时不显示按钮。
- 工具栏可换行，按钮最小高度 44px，滚动容器及反馈沿用深浅色 token。新增 `/code/` 样例与生成产物检查，新增 2 项构建逻辑测试验证原始片段保真、行号跳过、幂等、空代码和标签转义。
- Chrome 验证 1440/390/320px、Light/Dark、原始空白/实体/br 复制、空代码、行号排除、折叠代码、失败后重试、复制中的重复触发与焦点、键盘滚动、无 JS 及无 Clipboard API。手机截图与既有写作页/10 类页面回归通过，无浏览器脚本错误。
- 依赖镜像重试后安装成功；保留原锁文件中其他平台的 esbuild 条目，锁文件只增加 htmlparser2 的直接依赖声明，frozen/offline 安装通过。最终 lint、typecheck、干净构建（81 文件）、37 项测试、12 类页面及 Markdown 产物检查通过。

下一批：D4，完善图片响应式、原生懒加载、说明和尺寸约束，独立验证与提交。


### 2026-09-19：D4 图片展示与加载

- 构建阶段统一为正文 Markdown/原生 HTML 图片补齐 loading 与 decoding，保留显式设置，fetchpriority=high 在缺少 loading 时使用 eager；正文规则不改变页头/封面的加载策略。
- 独立段落中的单图或图片链接将 title 转为转义后的 figcaption；保留 alt、链接、尺寸和作者 HTML，已有 figure 不重复图注，多图/混排/代码内容不错误包裹。站内绝对 img src 通过 Hexo URL helper 适配子目录，picture/source/srcset 原样保留。
- 正文图片最大宽度为容器宽度、高度上限 min(80vh, 960px)，object-fit: contain 保持完整画面，小图不放大。增加 `/pictures/` 及本地横/竖 SVG 样例，覆盖说明、尺寸、picture、eager、失效 alt 和折叠图片。
- 新增 3 项图片逻辑测试和 1 项真实 Hexo 子目录/图注测试；样例纳入页面与 Markdown 产物检查。配置文档说明明确尺寸、长图退出高度约束、srcset 路径责任和故意失效样例；不引入图片运行时、尺寸探测下载或灯箱。
- Linux Chrome 验证 1440/390/320px、Light/Dark 下无页面溢出、图注不重复、竖图完整缩放、小图 120px、picture 响应来源、eager/decoding 保留、失效替代文字和无 JS 折叠图片；手机截图人工检查及既有写作/10 类页面回归通过。
- 最终 lint、typecheck、干净构建（84 文件）、41 项测试、13 类页面与 Markdown 产物检查通过。

下一批：D5，整理完整 MVP 示例内容及长文、无封面和边界场景，独立验证与提交。


### 2026-09-19：D5 完整示例与边界场景

- 新增 `/examples/` 作为当前示例入口，汇总阅读、排版、代码、图片和边界页面；只在 example 导航及示例翻译覆盖中加入入口，不改变主题默认导航。README 与开发文档链接到入口及新的 [示例站说明](examples.md)。
- 新增十节完整写作长文，包含封面、摘要、分类标签、表格、代码、图片说明、脚注、details 和前后文章链接；新增无封面无摘要文章、混排长标题/长字符串文章、空正文独立 Page 及英文 Page。旧历史内容和无标题文章继续保留用于迁移回归。
- 文档列出页面路径、源码映射、检查目的及故意失效图片，区分自动检查与 D7/D8 待执行的综合验收；空站点/配置覆盖/子目录场景继续使用隔离 Hexo 测试。
- 生成产物检查扩大到 19 类页面，校验示例入口 20 个站内链接均有目标，以及长文结构、紧凑卡片、空目录抑制和英文 UI。未为静态内容增加重复单元测试。
- lint、typecheck、干净构建（96 文件）、41 项测试、19 类页面、20 个站内入口链接及 Markdown 产物检查通过。Chrome 验证新增 6 类页面与首页在 1440/390/320px、Light/Dark 下无横向页面溢出，禁用 JS 的长文目录、英文 details 与导航可用；代码专项回归通过，无脚本错误。手机入口截图人工检查通过。

下一批：D7，执行并记录 Light/Dark、Mobile/Desktop、无 JS、键盘和 reduced-motion 综合验收；D6 已完成。


### 2026-09-19：D7 浏览器综合验收

- 新增可选浏览器验收脚本，覆盖 22 类页面、1440/768/390/320px 与系统深浅模式，共 176 组检查；另完成 66 组无 JS 检查，均无整页横向溢出。
- 键盘导航、跨断点焦点、主题偏好刷新保持/系统跟随/跨标签页同步、受限存储、复制空白保真及失败重试、无 JS 目录/脚注/details/表格/代码滚动和 reduced-motion 均通过，无页面脚本错误。
- 保存 [验收报告](validation/d7.md) 与 [逐项结果](validation/d7-browser.json)，开发文档补充重复运行方式。浏览器依赖不加入主题安装链路，主题代码与样式保持不变。
- 本次为 Linux Headless Chrome 视口模拟，剪贴板与存储拒绝使用替身；未验证真机、Safari/Firefox、屏幕阅读器或性能指标。历史友链外部图片及故意失效图片已在报告中说明。

- 最终 lint、typecheck、干净构建（96 文件）、41 项测试、19 类页面、20 个示例入口链接及 Markdown 产物检查通过。

下一批：D8，统计核心 JS/CSS gzip 大小并执行 Lighthouse 性能测量，记录实际环境和结果。


### 2026-09-19：D8 性能预算与 Lighthouse 基线

- 新增核心包 gzip 预算脚本并接入 `pnpm test`：JS 1334 bytes、CSS 5211 bytes（gzip level 9），分别低于 50 KB/40 KB 上限，JS 也低于 30 KB 争取目标；不混入图片或把离线压缩当作 HTTP 压缩。
- 独立 Lighthouse 12.8.2 + Chrome 151，静态服务加载首页/长文，mobile/desktop 各 3 次，共 12 次；保存设置、原始报告哈希、各次数据与中位数范围，见 [D8 报告](validation/d8.md) 和 [数据](validation/d8-performance.json)。不向主题添加浏览器依赖。
- 桌面 LCP 中位数为首页 1.588 秒、长文 0.870 秒；移动分别为 8.856 秒、4.354 秒，未达到 2.5 秒目标。CLS 全部低于 0.1，TBT 中位数均为 0 ms。导航测试没有测量 INP，不据此宣称其达标。
- 图片尺寸与传输量是审计建议的主要优化方向，静态服务也缺少文本压缩/长期缓存；新增 D8a 独立优化与复测任务，保留现有视觉结构。D8 完成测量不等于性能目标全部完成。

- 最终 lint、typecheck、干净构建（96 文件）、41 项测试、19 类页面、20 个示例入口链接、Markdown 与预算检查通过；额外使用超限临时包确认预算脚本会返回失败。

下一批：D8a，优化示例图片资源并复测移动 LCP；之后继续 D9 文档、版本与 PRD 验收。


### 2026-09-19：D8a 示例图片优化与复测

- 三张原始 JPG 生成按用途缩小的 WebP 副本，用于页头与四篇文章封面；共从 1580450 bytes 降至 219784 bytes（减少 86.1%）。保留原图、历史正文路径与原有布局，不修改主题样式或增加客户端依赖。
- 新增可重复转换脚本与示例维护说明，使用本机 cwebp 1.3.2、quality 78、method 6；生成文件随仓库提交，日常构建不需要编码器。既有单 URL 配置继续可用，未引入 srcset 配置。
- 同条件 Lighthouse 12 次复测：移动首页 LCP 中位数从 8.856 秒降至 1.883 秒、长文从 4.354 秒降至 1.658 秒；桌面为 0.591 秒、0.431 秒。12 次 LCP <2.5 秒、CLS <0.1；首页最慢 2.483 秒，余量较小，真实 INP 仍未测得。
- [对比报告](validation/d8a.md)、[性能数据](validation/d8a-performance.json) 与 [浏览器结果](validation/d8a-browser.json) 已保存，保留 D8 基线。176 组综合页面与 66 组无 JS 检查通过，深浅/桌面/手机截图人工检查通过，无页面脚本错误。

- 最终 lint、typecheck、干净构建（99 文件）、41 项测试、19 类页面、20 个示例入口链接、Markdown 与核心资源预算检查全部通过。

下一批：D9，补齐配置、开发和基础写作说明，整理版本与 CHANGELOG，逐项核对 PRD 验收；保留真实 INP 等尚未验证项目的说明。


### 2026-09-20：D9 文档、版本与 MVP 验收

- 整理 README、快速开始和基础写作说明，补齐主题生产依赖、站点 renderer、可选 feed、Front Matter、代码、图片、目录与基础发布检查；修复配置迁移表损坏及 B/D 阶段遗留的过时文案。
- 将沿用自上游的 0.2.5 改为 Syutoi 独立版本序列的 0.1.0；CHANGELOG 说明重置缘由、兼容变化与限制。保留原作者署名和许可证，保持 private，不创建 tag、不发布或部署。
- 新增 [MVP 逐项验收表](validation/mvp.md)，覆盖 PRD §52 的工程、页面、UI、文章与质量项；区分本地结果、CI 配置和未实测范围，保留真实 INP、远端 CI、真机/多浏览器及完整无障碍的限制。
- 独立临时博客按文档正常联网安装主题生产依赖，使用文档配置生成 28 文件；7 类页面、Markdown 扩展、相邻文章、图片及版本资源 URL 通过。未安装 feed，不复用仓库 node_modules 链接；离线缓存缺失和实际 peer 提示如实记录，未声称远端 clone/npm 发布验证。
- E1 所需分类、标签、404 和前后篇已由前序实现、浏览器验收及本轮独立安装检查覆盖，同步标记完成，避免重复开发。G1 的完整 customization/deployment 等仍留待后续。

- 最终 lint、typecheck、干净构建（99 文件）、41 项测试、19 类页面、20 个示例入口链接、Markdown 与 gzip 预算检查通过，核心生成资源无变动。

下一批：E2，增强桌面 sticky TOC、移动端折叠目录及可渐进增强的 scroll spy，保持无 JS 原生目录可用。


### 2026-09-20：E2 响应式目录与滚动高亮

- 文章与独立 Page 统一生成目录，桌面在原侧栏中 sticky，手机在正文前使用默认折叠的原生 details；CSS 仅显示当前断点对应的目录。无 JS 时固定、折叠和锚点链接仍可用，不复制正文标题 ID。
- 增加轻量滚动跟踪：requestAnimationFrame 合并检查，以顶部 96px 阅读线判断当前可见章节，页面底部处理最后章节；监听尺寸、图片加载、hash 和折叠变化，无 ResizeObserver 时保留事件回退。高亮带 aria-current、下划线和字重，不改写 URL/history、不抢焦点或平滑滚动页面。
- 长目录在自身面板滚动；跨 760/761px 断点时把目录焦点转到可见的对应链接，手机按需展开。保留 sidebar.enable/sidebar.toc/page.toc 开关，无标题不输出目录。
- 新增真实 Hexo 测试验证双呈现目标一致、重复标题锚点和全局关闭；新增可重复的浏览器专项脚本，并更新综合无 JS 目录流程。深浅、双向滚动、深链接刷新、内容尺寸变化、长目录、小屏、跨断点焦点、无 JS 与无 ResizeObserver 检查通过。
- 综合 176 组页面及 66 组无 JS 回归通过，无脚本错误；人工检查桌面浅色固定目录和手机深色折叠目录截图。证据见 [E2 报告](validation/e2.md)。更新配置、写作、迁移、开发说明与 Unreleased changelog。
- 最终 lint、typecheck、干净构建（99 文件）、42 项测试、19 类页面、20 个入口链接和 Markdown 检查通过。核心 JS/CSS gzip 为 2028/5382 bytes，预算通过；本批未重测 Lighthouse/真实 INP，不沿用旧数字作为当前实测。

下一批：E3，完善文章元信息、阅读时间及封面可选体验，先盘点已有实现再补齐缺项。


### 2026-09-20：E3 文章元信息、阅读时间与封面

- 文章头部集中显示带本地化标签的发布日期、跨日更新时间、作者和预计阅读时间，更新时间不在页尾重复。分类标签、日期格式、作者回退和独立 Page 的元信息边界保持既有规则。
- 新增构建时正文估时，使用已安装的 htmlparser2，不增加依赖或客户端代码。按中日韩文字 300 字/分钟、其他文字 200 词/分钟混合估算并向上取整，排除代码块、属性文字、隐藏元素和脚注标记等；空/纯代码/纯图片内容不输出估时，非空短文最少约 1 分钟。
- 增加全局 post.reading_time 与单篇 reading_time: false，支持 en/zh-CN/zh-TW 文案；全局关闭优先。文章和 Page 新增可选 cover_alt 并安全转义，无封面继续不生成图片和占位，列表图片仍使用装饰性空 alt。
- 新增算法边界及真实 Hexo 渲染测试，覆盖中英混排、实体/行内标签、排除规则、分钟边界、局部/全局开关、翻译、跨日更新、Page 分离和子目录封面；长文示例补充替代文字，配置、写作与 CHANGELOG 同步更新。
- lint、typecheck、干净构建（99 文件）、45 项测试、19 类页面、20 个入口链接、Markdown 与预算检查通过；48 组元信息浏览器场景、176 组页面及 66 组无 JS 回归通过，无脚本错误。人工检查手机深色有/无封面文章截图，见 [E3 报告](validation/e3.md)。JS/CSS 产物无变化，未重测 Lighthouse 或真实 INP。

下一批：E4，评估可选图片灯箱，确保关闭时不加载依赖，并保留原生图片与链接阅读方式。


### 2026-09-21：E4 可选灯箱评估

- 对比原生链接、dialog、自建查看器、PhotoSwipe 与 Fancybox，选择固定 PhotoSwipe 5.4.4 作为后续接入候选；[决策与证据](decisions/optional-lightbox.md) 区分评估完成与产品实现，主题本批仍没有灯箱开关或依赖。
- 新增可重复的隔离原型工具，候选包只放临时目录。实测 Lightbox/Core/CSS gzip 分别为 4467/16447/1460 bytes；1440/390px 下关闭原型无可选资源请求，Core 在打开时才加载，原生图片链接与无 JS 回退可用。
- 方向键切换、Escape 与焦点返回通过，但严格 Tab 循环出现 body 焦点，且阻断 Core 下载不会自动恢复原图导航。记录两项未通过结果，新增 E4a 接入任务，要求在适配层解决；不把原型当作完整灯箱上线。
- 当前真实主题图片页未请求灯箱资源；核心 JS/CSS、依赖与锁文件不变。明确目标图片尺寸、picture/srcset、作者已有链接、子目录、caption、按需加载与错误回退边界。

- 隔离原型实测及当前主题图片页网络检查完成，lint 与现有 gzip 预算通过；本批仅增加评估工具和文档，未重复未受影响的完整构建、45 项测试或 Lighthouse。

下一批：E4a，实施可选灯箱接入与失败回退，完成开启/关闭两种状态的功能、焦点和资源验收；之后继续 F1。


### 2026-09-21：E4a 可选图片灯箱

- 接入固定 PhotoSwipe 5.4.4，`lightbox.enable` 默认 false，单篇可关闭；仅在有合格正文图片时输出独立入口，首次点击才加载本地 Core/CSS。核心 JS/CSS 内容与 E4 前一致，不改变默认视觉结构。
- 原生图片链接作为无 JS、脚本/样式失败回退；使用固有尺寸或显式原图尺寸，保留 picture/srcset、普通文章链接与修饰点击语义，不提前下载全部懒图。提供纯文本图注、三语言控件、严格 Tab 循环、背景 inert、焦点返回和减少动态效果支持；修复开场动画期间过早关闭被忽略的问题。
- 新增 5 项测试，合计 50 项通过；lint/typecheck/clean/build/test 通过，生成 103 个文件。真实子目录站点 14 组灯箱场景通过，默认站点 176 组页面矩阵和 66 组无 JS 回归通过；实测范围与限制见 [E4a 验收](validation/e4a.md)。
- 可选入口/Core/CSS gzip 为 2175/17356/1628 bytes，独立纳入预算与生成一致性检查；保留分发许可证。配置、写作、开发和 Unreleased 记录同步更新，默认预览已恢复。

下一批：F1，定义搜索 provider 接口，验证 Pagefind 与 Hexo 生命周期的集成，再实现可选本地搜索。


### 2026-09-21：版本里程碑整理

- 按确认后的版本口径，将 MVP 提交 `5fc7789` 标记为 `v0.1.0`；当前完整阅读体验归入 `0.2.0`，版本整理提交标记为 `v0.2.0`，本次不创建 `v0.3.0`。
- 更新 package.json、README、当前使用文档与 CHANGELOG；历史 PRD 和验收报告保留当时记录。资源 URL 的版本参数随包版本更新，相关测试改为读取包版本，避免下次升级留下硬编码。
- lint、typecheck、干净构建、50 项测试与产物/预算检查通过；首页核心资源版本参数确认更新为 `0.2.0`，生成资源内容不变。
- 版本标签为本地附注标签，不推送远端、不发布 npm、不部署。下一批仍为 F1 本地搜索。


### 2026-09-21：F1 可选本地搜索

- 定义构建与浏览器搜索 provider 接口，首个实现为固定 Pagefind 1.5.2；默认 `search.provider: none`，开启 pagefind 后生成独立 `/search/` 页面，普通页面只有导航链接，首次查询才加载本地引擎和分片。
- 通过 Hexo generator 的内存路由接入索引，实际验证 public 写出前可用、watch 修改自动刷新、generate 替换旧分片、删除内容和禁用搜索的清理。不依赖额外 CLI 后处理；保留部署 root，只索引文章/Page 标题与正文，支持单篇退出并排除草稿、未来/密码内容。
- 实现三语言表单、状态、错误重试、每批 10 条结果、更多结果焦点和无 JS 归档回退；清空后忽略旧响应。真实故障测试发现并适配 Pagefind 的孤立初始化 rejection，使用固定版本和精确匹配校验，搜索错误仍正常传给界面。
- lint/typecheck/clean/build/test 通过，53 项测试、106 个生成文件；12 组搜索专项、176 组默认页面及 66 组无 JS 回归通过。搜索入口 JS/CSS gzip 为 1485/435 bytes，核心资源不变，Pagefind 引擎/索引另记成本。完整证据与边界见 [F1 验收](validation/f1.md)、[接入决策](decisions/local-search.md)。
- 更新配置、开发说明、依赖许可证和 Unreleased。包版本仍为 0.2.0，既有版本标签保持原指向；默认预览已恢复。

下一批：F2，定义 Comments Slot，以 Waline 为首个可选 adapter，关闭时无请求。


## F2 完成记录（2026-09-22）

- 定义默认关闭的 Comments Slot 和 `CommentsAdapter.mount/destroy` 接口，首个适配器为本地打包的 Waline 3.13.0。
- 新增 `comments.provider` / `comments.server_url`；文章和普通 Page 可启用，支持 `comments: false`。列表、搜索、404 和受保护内容不输出插槽。
- 点击后加载 Waline JS/CSS；支持重复挂载前清理、资源失败重试、无 JS 回退、中文/繁体/英文，以及主题明暗和移动端布局。
- 线程路径包含站点 root、忽略 index.html，查询参数与锚点不改变线程。示例站保持关闭，不连接公开评论服务。
- 完成配置/开发说明、架构决策、第三方许可清单和独立资源预算。核心 JS/CSS 内容及大小不变。
- 验证：lint、typecheck、clean/build、56 项测试、页面/Markdown/体积检查通过；12 个浏览器验收场景通过，无未捕获脚本错误。实际客户端连接本地模拟 API，尚未部署或验证真实 Waline 服务。

详情：[F2 验收记录](validation/f2.md)、[评论架构决策](decisions/comments.md)。

下一批：F3，完善 Open Graph/Twitter Card、结构化社交链接和 SEO 配置。


## 0.3.0 版本记录（2026-09-22）

- 将 package.json 与当前版本说明更新为 0.3.0，整理 F1/F2 Changelog。
- 构建和当时的 56 项测试通过；`c902412`（`chore: release Syutoi 0.3.0`）标记为 `v0.3.0`。既有标签保持不变。
- 后续 F3 作为独立提交进入 Unreleased，不移动 0.3.0 标签。

## F3 完成记录（2026-09-22）

- 元信息逻辑集中到 `lib/seo.cjs`，新增站点 SEO 配置和单页 Front Matter 覆盖；支持 title、description、canonical、分享图片/替代文字、noindex 与 Twitter 作者账号。
- Open Graph/Twitter Card 可独立关闭，分享图片有明确回退顺序；补齐页面 locale、文章分类和标签。canonical 与 og:url 保持一致，分页 URL 沿用现有生成路径。
- 404 和搜索固定 noindex；全局 noindex 不被单页 false 撤销。无效账号和不安全 URL 不输出。
- 社交链接支持 PRD 的 `type + url`，允许 name 覆盖；兼容既有 name/url 列表、结构化映射及旧字符串映射。不新增客户端资源。
- lint、clean、typecheck、build、61 项测试以及产物/Markdown/体积检查通过，所有 JS/CSS 构建产物保持不变。

详情：[F3 验收记录](validation/f3.md)、[SEO 与社交链接配置](configuration.md#seo-配置与单页覆盖f3)。

下一批：F4，通过 Hexo 插件验证 RSS、Sitemap，避免在主题重复实现。


## F4 完成记录（2026-09-22）

- 保留已使用的 hexo-feed 1.1.2 和 EJS 模板，在示例站安装 hexo-generator-sitemap 3.0.1；主题不新增 feed/sitemap 生成器。
- 修正 RSS 正文二次转义和 RSS 分类结构，补齐订阅自引用地址；Atom/JSON Feed 使用真实修改时间，RSS/Atom 汇总最新时间。保留现有三个订阅输出路径。
- 订阅入口同时检查站点开关与插件生成器，缺少插件时不生成无效链接。
- 真实插件集成覆盖根目录/子目录、自定义输出、草稿/未发布/未来文章、sitemap 排除、空站点、关闭 feed 与缺少插件。
- 严格 XML 解析与跨 feed 内容检查纳入 `pnpm test`；Sitemap 中的 URL 必须对应实际生成文件。
- lint、clean、typecheck、build、65 项测试、产物/Markdown/预算检查通过。示例三类 feed 各 20 篇，Sitemap 54 个有效地址，所有客户端产物不变。
- 配置与边界见 [订阅与站点地图](syndication.md)；明确 seo.noindex、sitemap:false 与 feed 包含范围互不等同，保留 hexo-feed 尚未声明 Hexo 8 peer 支持的说明。

详情：[F4 验收记录](validation/f4.md)。F 阶段任务完成。

下一批：G1，补齐 Beta 发布所需的上手、配置、写作、自定义、部署与迁移文档。


## G1 完成记录（2026-09-22）

- 补齐 customization/deployment，区分配置定制、源码定制、普通博客与仓库 Demo 的构建/发布目录；提供独立博客 GitHub Pages workflow 示例，并说明主题 submodule 与生产依赖安装。
- 更新 getting-started/configuration/writing/migration-from-shoka，补齐当前搜索、评论、SEO、Sitemap 行为和独立开关，清理 0.2.0 与“后续 E 阶段”等过时说明。
- README 增加文档入口，明确当前开发分支与 0.3.0 标签的范围；配置表补齐评论/SEO，澄清 branding.name 和导航 title 的位置。
- 临时独立博客用当前本地提交 clone、独立生产依赖安装及文档 YAML 生成成功；验证创建文章/Page、冻结锁文件重装、根目录与 /blog/、Markdown 脚注/任务列表、Pagefind 和 CNAME 复制。
- 25 个 YAML 示例可解析，50 个本地文件链接目标存在，git diff --check 通过。仅修改文档，未重跑无变化的全量代码测试或浏览器矩阵，也未推送、部署或修改 DNS。

详情：[G1 验证记录](validation/g1.md)。

下一批：G2，完善可发布的配置/内容迁移兼容清单与替代写法。


## G2 完成记录（2026-09-22）

- 以迁移前 a2a8440 的配置为基线，逐项列出兼容别名、手动迁移与移除字段；明确配置层优先级、旧 CDN/服务配置不会自动启用。
- 补齐 links/linksfile/media、Hexo 内置标签和未知标签的行为边界，提供配置前后对照、媒体/友情链接示例、私有 Markdown 替代写法与只读扫描命令。
- 明确旧生成 HTML、子目录路径、评论数据、原文来源和文章地址的迁移注意事项，不提供未经验证的自动替换。
- 32 项配置/渲染/标签测试通过，现有 Markdown 产物检查通过；3 个 YAML 片段、配置等价性、2 个实际标签渲染与 11 个文档本地链接校验通过。

详情：[G2 验证记录](validation/g2.md)。仅修改文档，主题代码与页面样式不变。

下一批：G3，完整验证核心页面的响应式与无障碍。


## G3 完成记录（2026-09-22）

- 完成核心七类页面及分类/标签详情的响应式、axe、键盘、无 JS 和文字放大验收，增加可复用的可选无障碍脚本。
- 修复页头地标遗漏和窄屏表格键盘访问；补充表格源文本保留、幂等、代码布局排除测试。
- 加深图片页头遮罩和粉色分类卡片渐变，调整装饰符与页头焦点颜色；保留图片、波浪、卡片与侧栏布局。
- 176 组响应式、66 组无 JS 回归通过；90 组 axe 扫描 0 violations，18 组跳转链接与 18 组 200% 文字检查通过，无 JS 普通表格方向键可用。
- 构建、lint、typecheck、67 项测试及产物/Markdown/订阅/预算检查通过；核心 JS 不变，CSS gzip 5419 bytes。
- 人工复核截图与图片/渐变对比度，明确 incomplete、桌面视口模拟及未覆盖屏幕阅读器/其他浏览器的边界，不宣称完整 WCAG 合规。

详情：[G3 验证记录](validation/g3.md)。

下一批：G4，在明确环境下验证性能预算并记录性能实测。


## G4 完成记录（2026-09-22）

- 核心 JS/CSS gzip 2028/5419 bytes，11 项核心及可选资源预算全部通过；不把图片或索引混入核心分母。
- 固定 Lighthouse 12.8.2 与 Chrome 151，首页/长文 × mobile/desktop × 3 次，两轮共 24 次顺序测量，记录环境、完整设置、请求、诊断及原始报告 SHA-256。
- 将默认导航 Logo 从 346076-byte 原 PNG 改为 2558-byte、96×96 WebP 副本；原图保留，显示尺寸和布局不变，维护命令及默认配置文档同步。
- 增加独立 web-vitals 交互测量工具；12 次脚本会话 INP 32–160ms。明确仅为所测操作的实验室结果，非真实用户 p75。
- 首页移动 LCP 中位数 2.785→2.557s，仍未达到 <2.5s；另外三组 LCP 及全部 CLS 达标。保留全部样本，不将预算通过等同于全部性能达标。
- 构建、lint、67 项测试及预算/产物检查通过，Logo 浏览器尺寸与解码验证通过。

详情：[G4 性能报告](validation/g4.md)。本阶段完成测量与预算验证，剩余 LCP 差距列为独立 G4a。

下一批：G4a，定位并优化移动端首页 LCP，然后同条件复测；之后继续 G5。


## G4a 完成记录（2026-09-22）

- 隔离试验比较 hero 同步解码与封面传输量：前者无明显改善、不采用；后者进入正式复测。两项单次探索数据独立保存，不混入验收样本。
- 保持置顶封面 800×1075、路径与裁剪方式，从原 JPG 以 q70 重编码，128642→108796 bytes（减少 15.43%）；原图与其他两张 WebP 保留，维护脚本按图记录质量参数。
- 同 G4 条件进行首页/长文 × mobile/desktop × 3 次：12 次 LCP 均 <2.5s、CLS 均 <0.1。保留移动首页第三次 CPU 性能警告，追加整组三次确认，不选择性替换原样本。
- 追加移动首页三次无警告，LCP 中位数 2.405s，范围 2.134–2.408s；原 G4 基线为 2.557s。明确实验室和机器波动边界。
- 构建、lint、67 项测试、产物/Markdown/订阅/预算检查，以及 176 组响应式和 66 组无 JS 回归通过。核心 JS/CSS 内容与预算不变。
- 人工对照 q78/q70 封面及手机/桌面截图，未见明显画质问题或布局变化；4000 预览已使用新封面。

详情：[G4a 报告](validation/g4a.md)。G4 的移动首页 LCP 差距已在本轮指定条件下解决。

下一批：G5，完成 Demo 与文档站，并验证 git clone 和可选 npm 安装方式。


## 0.4.0 版本记录（2026-09-22）

- 整理 F3/F4、G1–G4a 的 SEO、订阅、文档、迁移、无障碍与性能成果，统一当前版本说明为 0.4.0。
- 构建及 67 项测试、产物/Markdown/订阅/预算检查通过。
- v0.4.0 作为接下来视觉风格优化前的基线；G5/G6 尚未完成，不表示正式 1.0 或 npm 发布。
- 下一步先讨论主题配色、视觉层次与阅读风格，再按确定的方向实施；暂不继续 G5。


## V1：书台木色视觉第一版（2026-09-22）

- [x] 按用户确定的浅橡木、纸白、胡桃木与苔绿方向实现深浅色；保留图片页头、波浪、图文卡片与侧栏。
- [x] 将分类与页头的硬编码配色集中到 tokens，统一引用、表头、代码背景/高亮、链接与浏览器 theme-color。
- [x] 完成构建、lint、typecheck、67 项测试及预算检查；176 组响应式、66 组无 JS、90 组 axe 和最终 12 组颜色复核通过。
- [ ] 持续根据实际浏览反馈调整风格；木色、头像与田园配图已落地，当前恢复推进 G5，后续再进入 G6。

方案、色板与验证边界：[书台木色方案](design/wood-palette.md)。v0.4.0 标签保留为改版前基线。

## 示例内容整理（2026-09-22）

- [x] 清理旧文档、课程笔记与占位文章，公开保留三篇展示文章。
- [x] 按书台手记、阅读与写作整理目录和分类，更新日期并保留明确的文章链接。
- [x] 将边界示例改为独立页面，迁移兼容材料移入隔离测试夹具。
- [x] 清理生成目录后重建；68 项测试、页面与订阅检查及 lint 通过；浏览器 168 项布局组合、63 项无 JavaScript 检查及交互检查通过。

## Demo 文档中心（2026-09-22）

- [x] 新增主题文档分类、文档首页和多语言导航入口。
- [x] 将现有使用文档作为唯一正文来源，构建时同步，开发时监听更新。
- [x] 提供八篇指南、阅读顺序、固定链接与实际示例入口。
- [x] 增加文档链接转换测试及生成页面、站内链接和锚点检查。
- [x] 构建、69 项测试与 lint 通过；文档页面 60 项桌面/手机明暗布局检查、无 JS 导航及首页排序验证通过。

## 可选 Markdown 扩展

- [x] 图片尺寸 attrs 与 GitHub 风格提示块；Demo 启用，普通博客可选。
- [x] 主题明暗样式、使用文档和迁移说明。
- [x] 隔离 Hexo 回归：子目录、图注、属性限制、转义、嵌套引用与禁用。
- [x] 桌面/手机明暗共 6 组浏览器与正文无障碍检查通过，头像实际 100×100，无 JS 仍可阅读。

## G5a：独立 Git 安装验收（2026-09-23）

- 验证提交 `d747660`，通过本地 Git clone 安装到隔离 Hexo 博客；未借用工作区 node_modules。
- 按快速开始 YAML 安装主题生产依赖与站点插件，冻结锁文件重装通过；无需构建主题源码。
- 根目录/子目录 × 默认/可选扩展共 4 组通过，每组检查 9 类页面及其 JS/CSS 文件；另验图片尺寸、图注、脚注、任务列表、提示块、灯箱标记与搜索页。
- 新增可重复运行的 `pnpm check:install`；结果和日志留在其输出的临时目录，不纳入日常测试或 CI 的网络依赖。
- 文档明确当前开发分支与 v0.4.0 标签的差别；没有创建 tag、推送、部署或发布 npm。

下一批：G5c，核实线上 Demo／文档中心的实际部署结果；随后 G5d 测量新版性能，再进入 G6 发布准备。

## G5c：线上 Demo／文档验收（2026-09-23）

- 对 https://hexo.syutoi.com/ 执行只读验收，58 次请求、213 项检查通过；覆盖核心页面、8 篇指南、站内文档链接/锚点、资源 MIME、订阅和 Sitemap。
- canonical 使用 index.html 的页面按等价 URL 验证，实际目标可访问；未知路径返回真正的 HTTP 404 和主题错误页。
- 线上核心 JS/CSS 与当前本地预构建产物一致；不据此推断确切部署 commit。前次网络连接中断经有限重试和重新验收后通过。
- Chrome 151：首页、文档首页、扩展示例 × 1440/390px × light/dark 共 12 组通过，无脚本错误；扩展头像宽度 100px，无 JS 文档导航通过。
- 新增 `pnpm check:online` 与验收记录。没有推送或触发部署，没有复测 Lighthouse。

下一批：G5d，以当前木色视觉、田园配图与文档集测量新的性能基线，再评估发布版本。

## G5d：新版性能基线（2026-09-23）

- 测量提交 `0eac0de`；当前 11 篇文章、木色样式、田园配图与新头像。未改变素材或布局。
- 同原方法，首页/长文 × mobile/desktop × 3 次，12 次 Lighthouse 无运行警告。核心 JS/CSS gzip 2026/5756 bytes，全部 11 项资源预算通过。
- 移动首页 LCP 中位数 3.013s，三次均超出 2.5s；其他组 LCP、全部 CLS 达标。性能评分不代表所有指标已通过。
- 12 次实验室交互 INP 为 32–184ms，无页面异常，不代表真实用户 p75。
- 当前首页传输约 1.066MB，包含 748349-byte 头像和 155078-byte 页头图；仍需隔离验证其对 LCP 的影响，新增 G5d1。
- 原始数据、环境、请求、哈希和复现步骤见 [G5d 报告](validation/g5d.md)，旧 G4/G4a 数据保持不变。

下一批：G5d1，保留配图内容与布局，隔离验证并降低移动首页图片成本，然后同条件复测。

## 0.5.0 版本整理（2026-09-26）

- 统一 package.json、README、当前使用文档与 CHANGELOG 为 0.5.0，历史验收记录保留当时版本与提交信息。
- 收录木色与田园视觉、文档中心、可选图片属性与提示块，以及独立 Git 安装、线上验收和当前性能基线。
- 移动首页 LCP 中位数 3.013s 尚未达到 <2.5s，G5d1 保持待办；G5/G6 不标记全部完成。
- 本次为本地 Git 版本提交与附注标签，不代表 npm 发布、远端标签或站点部署。
- 验证：lint、typecheck、clean 后构建、73 项测试与生成站点检查通过；资源体积预算通过。
